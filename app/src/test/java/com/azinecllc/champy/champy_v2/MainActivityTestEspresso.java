package com.azinecllc.champy.champy_v2;

/*import android.support.test.espresso.Espresso;
import android.support.test.runner.AndroidJUnit4;
import static android.support.test.espresso.Espresso.onView;
import static android.support.test.espresso.action.ViewActions.click;
import static android.support.test.espresso.action.ViewActions.closeSoftKeyboard;
import static android.support.test.espresso.action.ViewActions.typeText;
import static android.support.test.espresso.assertion.ViewAssertions.matches;
import static android.support.test.espresso.matcher.ViewMatchers.isDisplayed;
import static android.support.test.espresso.matcher.ViewMatchers.withId;
import static android.support.test.espresso.matcher.ViewMatchers.withText;
import android.support.test.espresso.action.ViewActions;
import android.support.test.espresso.matcher.ViewMatchers;
import android.support.test.rule.ActivityTestRule;
import android.support.test.runner.AndroidJUnit4;
import android.test.ActivityInstrumentationTestCase2;*/

import android.test.suitebuilder.annotation.LargeTest;

import org.junit.runner.RunWith;
import org.junit.runners.JUnit4;

@RunWith(/*Android*/JUnit4.class)
@LargeTest
public class MainActivityTestEspresso {

    /*@Rule
    public ActivityTestRule<MainActivity> mActivityRule = new ActivityTestRule<>(
            MainActivity.class);

    @Test
    public void setUp() {
        //mActivityRule.getClass().getResource(String.valueOf(R.id.textViewChallenges));

        onView(withId(R.id.textViewChallenges)).check(matches(isDisplayed()));
        onView(withId(R.id.textViewWins)).check(matches(isDisplayed()));
        onView(withId(R.id.textViewTotal)).check(matches(isDisplayed()));

        onView(withId(R.id.imageView_challenges_logo)).check(matches(isDisplayed()));
        onView(withId(R.id.imageView_wins_logo)).check(matches(isDisplayed()));
        onView(withId(R.id.imageView_total_logo)).check(matches(isDisplayed()));

        onView(withId(R.id.imageView_challenges_animation)).check(matches(isDisplayed()));
        onView(withId(R.id.imageView_wins_animation)).check(matches(isDisplayed()));
        onView(withId(R.id.imageView_total_animation)).check(matches(isDisplayed()));

        onView(withId(R.id.cards)).check(matches(isDisplayed()));

        onView(withId(R.id.fabPlus)).check(matches(isDisplayed()));
        onView(withId(R.id.fabPlus)).perform(click());
    }


    @Test
    public void onBackPressedTest() {
        Espresso.pressBack();
    }*/

}